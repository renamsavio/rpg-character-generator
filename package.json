{
  "name": "rpg-character-generator",
  "version": "1.0.0",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "fastapi": "^0.68.0",
    "uvicorn": "^0.15.0",
    "pydantic": "^2.0.0",
    "pydantic-settings": "^2.0.0",
    "sqlalchemy": "^1.4.0",
    "databases": "^0.5.0"
  }
}
